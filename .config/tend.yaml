bot_name: worktrunk-bot
harness: claude-interactive
setup:
- uses: ./.github/actions/claude-setup
secrets:
  allowed:
  - CODECOV_TOKEN
workflows:
  ci-fix:
    watched_workflows:
    - ci
    - publish-docs
